function cores(valor: string) { // fabrica de decoradores
    return function (alvo) { // este é um dos decoradores
        // funcao que implementa uma funcionalidade usando 
        // valor e alvo
    }
}